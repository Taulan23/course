function отправитьФорму() {
    // Код функции...
}

function обновитьДанные() {
    // Код функции...
}

// Добавляем новую функцию
function посмотреть() {
    // Здесь мы можем добавить код для отображения данных
    // Например, отправить AJAX-запрос на сервер и показать результат
    fetch('../scripts/проверка_лимитов_загрузки.php')
        .then(response => response.text())
        .then(data => {
            alert('Результаты проверки лимитов загрузки:\n' + data);
        })
        .catch(error => {
            console.error('Ошибка:', error);
            alert('Произошла ошибка при получении данных');
        });
}

// Другие функции...
